#include<iostream>
using namespace std;
int main(){
    int x;
    //arrays can be used to store multiple values in a single variable name
    // int a[10]; here the size of the size of varaible a which is array is 10 that is it can accomdate 10 values 
//indexing starts from 0 and not from 1
//the first element has index 0 and we can even start the index from last where the index of last element is -1
//array is a data structurw which can represent collection of similar data type items.


//to declare an array first we define data type name of the array and the size of the array are three parameters we need to consider

}